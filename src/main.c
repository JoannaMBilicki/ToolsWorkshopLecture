#include <stdio.h>

int main(int argc, char** argv) {

    // Print off a hello world message
    printf("Hello colorful world from processor %s, rank %d out of %d processors\n",
           "0", 0, 1);
}